package org.example;

import org.example.player.PlayerApp;

/** Entry point for the playerTerm terminal player. */
public class Main {
    public static void main(String[] args) {
        System.exit(PlayerApp.run(args));
    }
}
